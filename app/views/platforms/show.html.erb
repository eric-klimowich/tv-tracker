<h1> <%= @platform.name %> </h1>

<h3> <%= number_to_currency(@platform.subscription_cost, unit: "$")%> </h3>


<%= button_to "All Platforms", platforms_path, method: :get %>
<%= button_to "Logout", login_path, method: :delete, data: { confirm: "Are you sure you want to log out?"} %>
